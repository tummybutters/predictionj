# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Supabase (server-side only for now)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# AI
AI_PROVIDER=gemini
GOOGLE_AI_API_KEY=
# Reserved for future multi-provider support:
OPENAI_API_KEY=

# Micro-task handle naming (defaults to OpenAI if available, else Gemini)
HANDLE_AI_PROVIDER=openai
HANDLE_AI_MODEL=gpt-5-nano

# App crypto (used to encrypt provider private keys at rest)
# Provide either 64 hex chars (32 bytes) or base64 of 32 bytes.
APP_ENCRYPTION_KEY=

# Kalshi
# Defaults to https://api.elections.kalshi.com if unset.
# For demo/sandbox environments you can use https://demo-api.kalshi.co
KALSHI_API_ORIGIN=
